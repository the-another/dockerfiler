/**
 * TypeScript type definitions for Dockerfile Generator
 */

// TODO: Define base configuration interfaces
export interface BaseConfig {
  // Placeholder for future configuration types
}

// TODO: Define platform-specific configuration interfaces
export interface PlatformConfig {
  // Placeholder for future platform types
}

// TODO: Define command argument interfaces
export interface CommandArgs {
  // Placeholder for future command argument types
}

// TODO: Define error type definitions
export interface AppError {
  // Placeholder for future error types
}

// TODO: Define template data interfaces
export interface TemplateData {
  // Placeholder for future template data types
}
